const { Telegraf } = require('telegraf');

// Инициализация системы SERIES(CG)
const bot = new Telegraf('ТВОЙ_ТОКЕН');

// Сообщение от лица системы
bot.start((ctx) => {
  ctx.reply(
    '🛰 **Система SERIES(CG) приветствует вас!**\n\n' +
    'Вы запустили учебный модуль для начинающих разработчиков.\n' +
    'Данный бот создан, чтобы показать основы JavaScript в связке с Python.\n\n' +
    'Используйте /help, чтобы увидеть список доступных модулей.'
  );
});

bot.help((ctx) => {
  ctx.reply(
    '📜 **Справочный центр SERIES(CG):**\n' +
    '1. /info — О проекте\n' +
    '2. /code — Как я устроен?\n' +
    '3. /status — Проверка систем'
  );
});

bot.command('info', (ctx) => {
  ctx.reply(
    '🤖 **О проекте:**\n' +
    'Этот бот — часть экосистемы SERIES(CG) Studios. Наша цель: сделать программирование доступным для каждого.'
  );
});

// Запуск протокола
bot.launch().then(() => {
  console.log('--- [ СИСТЕМА SERIES(CG) ОНЛАЙН ] ---');
});
